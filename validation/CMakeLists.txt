# Validation reuses the same compiled simulation classes as the application.
file(WRITE "${CMAKE_CURRENT_BINARY_DIR}/multithreaded.txt" "${Geant4_multithreaded_FOUND}")
foreach(tool geometry_snapshot geometry_audit chain_boundaries)
  add_executable(${tool} ${tool}.cc)
  target_link_libraries(${tool} PRIVATE cygno_sim)
endforeach()

add_executable(source_checks source_checks.cc)
target_link_libraries(source_checks PRIVATE cygno_sim)
add_test(NAME source_and_mass COMMAND source_checks)
add_test(NAME source_and_mass_legacy COMMAND source_checks legacy-25x3)
add_test(NAME source_and_mass_11x7 COMMAND source_checks cygno-11x7-v1)
set_tests_properties(source_and_mass source_and_mass_legacy source_and_mass_11x7 PROPERTIES SKIP_RETURN_CODE 77)
add_executable(layout_profiles layout_profiles.cc)
target_compile_features(layout_profiles PRIVATE cxx_std_17)
target_include_directories(layout_profiles PRIVATE "${PROJECT_SOURCE_DIR}")
add_test(NAME layout_profiles COMMAND layout_profiles)
add_test(NAME chain_boundaries COMMAND chain_boundaries)
find_package(Python3 COMPONENTS Interpreter REQUIRED)
add_test(NAME simulation_python COMMAND ${Python3_EXECUTABLE} -m unittest discover
  -s tests -p test_simulation.py -v)
set_tests_properties(simulation_python PROPERTIES WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}")
option(CYGNO_TEST_PYTHON_ANALYSIS "Test uproot analysis (install study/requirements-analysis.txt)" OFF)
if(CYGNO_TEST_PYTHON_ANALYSIS)
  add_test(NAME offline_python COMMAND ${Python3_EXECUTABLE} -m unittest discover -s tests -v)
  set_tests_properties(offline_python PROPERTIES WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}")
  if(CYGNO_BUILD_ANALYSIS)
    set_tests_properties(offline_python PROPERTIES ENVIRONMENT "CYGNO_REFERENCE_BUILD=${PROJECT_BINARY_DIR}")
  endif()
endif()
add_test(NAME layout_geometry COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/run_checks.py layouts ${PROJECT_BINARY_DIR})
add_test(NAME geometry COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/run_checks.py geometry ${PROJECT_BINARY_DIR})
add_test(NAME transport COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/run_checks.py transport ${PROJECT_BINARY_DIR})
if(CYGNO_BUILD_ANALYSIS)
  add_test(NAME study_runner COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/test_study_runner.py ${PROJECT_BINARY_DIR})
  set_tests_properties(study_runner PROPERTIES TIMEOUT 180)
  add_test(NAME long_lived_decay COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/test_long_lived.py ${PROJECT_BINARY_DIR})
  set_tests_properties(long_lived_decay PROPERTIES TIMEOUT 180)
  add_test(NAME source_matrix COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/test_source_matrix.py ${PROJECT_BINARY_DIR})
  add_test(NAME analysis COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/test_analysis.py ${PROJECT_BINARY_DIR})
  add_test(NAME layout_analysis COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/test_layout_analysis.py ${PROJECT_BINARY_DIR})
  add_test(NAME root_geometry COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/check_analysis.py ${PROJECT_BINARY_DIR}/validation-results/geometry/placements.tsv ${PROJECT_BINARY_DIR}/validation-results/root-geometry)
  set_tests_properties(root_geometry PROPERTIES DEPENDS geometry)
endif()
set_tests_properties(source_and_mass source_and_mass_legacy source_and_mass_11x7 layout_profiles layout_geometry geometry transport PROPERTIES TIMEOUT 180)

# Optional manual external reproducer, deliberately not a passing test.
add_executable(external_decay_probe EXCLUDE_FROM_ALL external_decay_probe.cc)
target_link_libraries(external_decay_probe PRIVATE ${Geant4_LIBRARIES})

# Optional small transport parity; never enabled implicitly for a production campaign.
option(CYGNO_TEST_COMPACT_TRANSPORT "Run 30-primary raw/compact/both parity" OFF)
if(CYGNO_TEST_COMPACT_TRANSPORT)
  add_test(NAME compact_transport COMMAND ${Python3_EXECUTABLE} -m unittest discover
    -s tests -p test_compact.py -k TransportTests -v)
  set_tests_properties(compact_transport PROPERTIES WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}"
    ENVIRONMENT "CYGNO_TRANSPORT_BUILD=${PROJECT_BINARY_DIR}" TIMEOUT 600)
endif()
